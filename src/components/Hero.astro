---
/**
 * Hero — ホーム §1「HERO」本実装
 * 設計書: 11_HPリニューアル_設計書.md §1 §1 / DESIGN.md §1 積み木モチーフ
 *
 * 要素:
 *   - 大見出し（ブランドストーリーの一行）
 *   - サブ（"中小企業のAIネイティブ化を伴走"）
 *   - ダブルCTA（AI駆動経営診断 / 無料個別相談）
 *   - 右側に積み木ビジュアル（4ブロックを段々に積む / SVG＋CSS animation）
 *
 * デザイン方針（DESIGN.md §1 / §10.5）:
 *   - 青紫グラデは禁止
 *   - オレンジ大面積禁止（積み木は accent-soft 優位）
 *   - 見出しは明朝（Shippori Mincho）
 */
---

<div class="hero-block">
  <div class="hero-content">
    <p class="kicker">中小企業のための AI内製化伴走ブランド</p>
    <h1 class="hero-title">
      <span class="hero-title-step">1段ずつ、着実に。</span>
      <span class="hero-title-goal">AI駆動経営へ。</span>
    </h1>
    <p class="hero-lead">
      AI駆動経営とは、<strong>AI を、自分たちで事業モデルやあらゆる業務の中心に組み込める経営スタイル</strong>のことです。<br />
      ツミキAIは、中小企業経営者と一緒に
      AI内製化を<strong>"積み上げる"</strong>伴走ブランドです。
    </p>

    <div class="hero-cta-row">
      <a class="hero-cta hero-cta--primary" href="/diagnose/">
        <span class="hero-cta-label">AI駆動経営診断を受ける（無料）</span>
      </a>
      <a class="hero-cta hero-cta--secondary" href="/contact/">
        <span class="hero-cta-label">無料個別相談で話す</span>
      </a>
    </div>
    <p class="hero-cta-tertiary">
      <a href="#product-matrix">サービス全体を先に見る →</a>
    </p>

    <p class="hero-footnote">
      診断 → 講座 → 伴走の道筋で、自走まで並走します。
      中小企業のIT現場を見続けてきた代表が、千葉から発信しています。
    </p>
  </div>

  <div class="hero-visual" aria-hidden="true">
    <div class="stack">
      <div class="block block--4">
        <span class="block-num">04</span><span class="block-name">AIネイティブ</span>
      </div>
      <div class="block block--3">
        <span class="block-num">03</span><span class="block-name">戦略を描く</span>
      </div>
      <div class="block block--2">
        <span class="block-num">02</span><span class="block-name">活用を広げる</span>
      </div>
      <div class="block block--1">
        <span class="block-num">01</span><span class="block-name">土台を組む</span>
      </div>
    </div>
  </div>
</div>

<style>
  .hero-block {
    display: grid;
    grid-template-columns: 1.2fr 1fr;
    gap: var(--space-16);
    align-items: center;
    min-height: 520px;
  }

  /* Content */
  .hero-content {
    display: flex;
    flex-direction: column;
    gap: var(--space-6);
  }
  .kicker {
    font-family: var(--font-en);
    font-size: var(--text-caption);
    font-weight: var(--weight-semibold);
    color: var(--color-text-muted);
    letter-spacing: 0.08em;
    text-transform: uppercase;
    margin: 0;
  }
  .hero-title {
    font-family: var(--font-jp-serif);
    font-size: var(--text-display);
    line-height: var(--leading-display);
    font-weight: var(--weight-bold);
    letter-spacing: var(--tracking-heading);
    color: var(--color-text);
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: var(--space-1);
  }
  .hero-title-step {
    display: block;
    color: var(--color-text);
    /* "1" を accent でハイライト */
  }
  .hero-title-step::first-letter {
    color: var(--color-accent);
    font-family: var(--font-en);
  }
  .hero-title-goal {
    display: block;
    color: var(--color-primary);
  }
  .hero-lead {
    font-size: var(--text-body-lg);
    line-height: var(--leading-body-lg);
    color: var(--color-text-body);
    margin: 0;
  }
  .hero-lead strong {
    color: var(--color-text);
    font-weight: var(--weight-bold);
  }

  /* CTA */
  .hero-cta-row {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-4);
    margin-top: var(--space-2);
  }
  .hero-cta {
    display: inline-flex;
    align-items: center;
    padding: var(--space-4) var(--space-8);
    border-radius: var(--radius-md);
    text-decoration: none;
    box-shadow: var(--shadow-button);
    transition:
      transform var(--duration-hover) var(--ease-out-quint),
      box-shadow var(--duration-hover) var(--ease-out-quint);
  }
  .hero-cta:hover {
    transform: translateY(-2px);
    box-shadow: var(--shadow-md);
  }
  .hero-cta--primary {
    background: var(--color-primary);
    color: #fff;
  }
  .hero-cta--primary:hover {
    color: #fff;
  }
  .hero-cta--secondary {
    background: #fff;
    color: var(--color-primary);
    border: 1px solid var(--color-primary);
  }
  .hero-cta-label {
    font-family: var(--font-jp);
    font-size: var(--text-body);
    font-weight: var(--weight-semibold);
  }

  .hero-cta-tertiary {
    margin: var(--space-1) 0 0;
    font-size: var(--text-body-sm);
  }
  .hero-cta-tertiary a {
    color: var(--color-text-muted);
    text-decoration: none;
    border-bottom: 1px dashed var(--color-text-muted);
    padding-bottom: 1px;
  }
  .hero-cta-tertiary a:hover {
    color: var(--color-primary);
    border-bottom-color: var(--color-primary);
  }

  .hero-footnote {
    font-size: var(--text-caption);
    color: var(--color-text-muted);
    margin: 0;
    padding-top: var(--space-4);
    border-top: 1px solid var(--color-border);
  }

  /* ========================================
   * Visual — 積み木を段々に積むイラスト
   * DESIGN.md §1 工芸品モチーフ / §5「積み木」表現
   * ======================================== */
  .hero-visual {
    display: flex;
    align-items: flex-end;
    justify-content: center;
    padding: var(--space-8);
  }
  .stack {
    position: relative;
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: var(--space-2);
    width: 100%;
    max-width: 420px;
    animation: stack-reveal var(--duration-reveal) var(--ease-out-quint) both;
  }
  .block {
    display: flex;
    align-items: center;
    gap: var(--space-3);
    padding: var(--space-4) var(--space-6);
    border-radius: var(--radius-md);
    color: var(--color-text);
    box-shadow: var(--shadow-sm);
    animation: block-in var(--duration-reveal) var(--ease-out-quint) both;
  }
  .block-num {
    font-family: var(--font-en);
    font-weight: var(--weight-bold);
    font-size: var(--text-body);
    font-feature-settings: "tnum" 1;
    color: var(--color-text-muted);
    letter-spacing: 0.02em;
  }
  .block-name {
    font-family: var(--font-jp-serif);
    font-weight: var(--weight-semibold);
    font-size: var(--text-body);
    white-space: nowrap;
  }
  .block--1 {
    width: 96%;
    background: var(--color-bg-warm);
    border: 1px solid var(--color-border);
    animation-delay: 0ms;
  }
  .block--2 {
    width: 78%;
    background: var(--color-bg-subtle);
    border: 1px solid var(--color-border);
    margin-left: var(--space-6);
    animation-delay: 120ms;
  }
  .block--3 {
    width: 62%;
    background: var(--color-accent-soft);
    color: #B84A1E;
    margin-left: var(--space-12);
    animation-delay: 240ms;
  }
  .block--4 {
    width: 44%;
    background: var(--color-primary);
    color: #fff;
    margin-left: var(--space-16);
    animation-delay: 360ms;
    box-shadow: var(--shadow-md);
  }

  @keyframes stack-reveal {
    from {
      opacity: 0;
    }
    to {
      opacity: 1;
    }
  }
  @keyframes block-in {
    from {
      opacity: 0;
      transform: translateY(12px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  /* ========================================
   * Responsive
   * ======================================== */
  @media (max-width: 1023px) {
    .hero-block {
      grid-template-columns: 1fr;
      gap: var(--space-8);
      min-height: auto;
    }
    .hero-visual {
      padding: 0;
    }
    .stack {
      max-width: 100%;
    }
  }

  @media (max-width: 640px) {
    .hero-title {
      font-size: var(--text-h1);
    }
    .block--1 {
      width: 100%;
    }
    .block--2 {
      width: 85%;
      margin-left: var(--space-4);
    }
    .block--3 {
      width: 70%;
      margin-left: var(--space-8);
    }
    .block--4 {
      width: 55%;
      margin-left: var(--space-12);
    }
  }
</style>
